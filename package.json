{
  "name": "vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "cd backend && npm install && npm start",
    "backend": "cd backend && npm run dev"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  }
}
